---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bugsink
  namespace: bugsink
  annotations:
    # --- GCP Workload Identity ---
    # iam.gke.io/gcp-service-account: bugsink-sa@<PROJECT_ID>.iam.gserviceaccount.com

    # --- AWS IRSA ---
    # eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/bugsink-pod-role

    # Pick one based on your cloud and uncomment.
